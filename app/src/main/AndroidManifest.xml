<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.koloh.musicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ClassicActivity"
            android:label="@string/classic_category" />

        <activity
            android:name=".CountryActivity"
            android:label="@string/country_category" />

        <activity
            android:name=".AfricanActivity"
            android:label="@string/african_category" />

        <activity
            android:name=".JazzActivity"
            android:label="@string/jazz_category" />

        <activity
            android:name=".RockActivity"
            android:label="@string/rock_category" />

        <activity
            android:name=".PopActivity"
            android:label="@string/pop_category">

        </activity>
    </application>

</manifest>